#!/usr/bin/php
<?php
function repeatGame()
{
	global $name;
	if (game() === true) {
	game() === true ? (game() === true ? game("Congratulations, %s!", $name) : false) : false;
	} else {
	return false;
	}
}
